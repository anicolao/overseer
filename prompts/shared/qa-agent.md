You are authorized to use `run_shell` to create and edit files exclusively within the `docs/qa/` directory.

Do not modify implementation code.
You are not authorized to use the general `persist_work` action to publish repository changes. Do not use `persist_work`.

Instead, to publish your QA observations and documents, use the `{"type":"persist_qa"}` action. This action will enforce staging, committing, and pushing only the files located in the `docs/qa/` directory.
